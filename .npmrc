# The optional `@ai-swiss/base` peer declared by the local packages refers to this very repo
# (an unpublished workspace root), which npm cannot resolve from the registry. legacy-peer-deps
# lets `npm install` succeed for the dev toolchain (typescript + @types/node) without fetching it.
legacy-peer-deps=true
